# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: gity

before:
  hooks:
    - go mod download

builds:
  - &gity_build
    id: gity-server
    main: ./cmd/server
    binary: gity-server
    targets:
      - linux_amd64_v1
      - linux_arm64
      - darwin_amd64_v1
      - darwin_arm64
      - windows_amd64_v1
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w -X github.com/lyonbrown4d/gity/internal/debug.VersionOverride={{ .Version }}
    mod_timestamp: "{{ .CommitTimestamp }}"
  - <<: *gity_build
    id: gity-migration
    main: ./cmd/migration
    binary: gity-migration
  - <<: *gity_build
    id: gity-worker
    main: ./cmd/worker
    binary: gity-worker
  - <<: *gity_build
    id: gity-standalone
    main: ./cmd/standalone
    binary: gity-standalone
  - <<: *gity_build
    id: gity-runner
    main: ./cmd/runner
    binary: gity-runner

upx:
  - enabled: true
    ids:
      - gity-server
      - gity-migration
      - gity-worker
      - gity-standalone
      - gity-runner
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    compress: best
    lzma: true
  - enabled: true
    ids:
      - gity-server
      - gity-migration
      - gity-worker
      - gity-standalone
      - gity-runner
    goos:
      - windows
    goarch:
      - amd64
    compress: best
    lzma: true

archives:
  - id: gity
    ids:
      - gity-server
      - gity-migration
      - gity-worker
      - gity-standalone
      - gity-runner
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
      {{- with .Arm }}v{{ . }}{{ end -}}
      {{- if not (eq .Amd64 "v1") }}{{ .Amd64 }}{{ end -}}
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - README.md

nfpms:
  - &gity_nfpm
    id: gity-server
    package_name: gity-server
    ids:
      - gity-server
    vendor: Gity
    homepage: https://github.com/lyonbrown4d/gity
    maintainer: lyonbrown4d
    description: Gity API server.
    bindir: /usr/bin
    dependencies:
      - git
    formats:
      - deb
      - rpm
      - apk
    contents:
      - src: README.md
        dst: /usr/share/doc/gity-server/README.md
  - <<: *gity_nfpm
    id: gity-migration
    package_name: gity-migration
    ids:
      - gity-migration
    description: Gity database migration command.
    dependencies: []
    contents:
      - src: README.md
        dst: /usr/share/doc/gity-migration/README.md
  - <<: *gity_nfpm
    id: gity-worker
    package_name: gity-worker
    ids:
      - gity-worker
    description: Gity background worker.
    contents:
      - src: README.md
        dst: /usr/share/doc/gity-worker/README.md
  - <<: *gity_nfpm
    id: gity-standalone
    package_name: gity-standalone
    ids:
      - gity-standalone
    description: Gity single-process runtime.
    contents:
      - src: README.md
        dst: /usr/share/doc/gity-standalone/README.md
  - <<: *gity_nfpm
    id: gity-runner
    package_name: gity-runner
    ids:
      - gity-runner
    description: Gity CI runner agent.
    contents:
      - src: README.md
        dst: /usr/share/doc/gity-runner/README.md

dockers_v2:
  - &gity_docker
    id: gity-server
    ids:
      - gity-server
    images:
      - ghcr.io/lyonbrown4d/gity-server
    tags:
      - "{{ .Version }}"
      - "{{ .Tag }}"
    dockerfile: build/package/Dockerfile
    platforms:
      - linux/amd64
      - linux/arm64
    build_args:
      BINARY: gity-server
    labels:
      org.opencontainers.image.title: gity-server
      org.opencontainers.image.description: Gity API server
      org.opencontainers.image.source: https://github.com/lyonbrown4d/gity
      org.opencontainers.image.revision: "{{ .FullCommit }}"
      org.opencontainers.image.version: "{{ .Version }}"
    sbom: false
    disable: "{{ .IsSnapshot }}"
  - <<: *gity_docker
    id: gity-migration
    ids:
      - gity-migration
    images:
      - ghcr.io/lyonbrown4d/gity-migration
    build_args:
      BINARY: gity-migration
    labels:
      org.opencontainers.image.title: gity-migration
      org.opencontainers.image.description: Gity database migration command
      org.opencontainers.image.source: https://github.com/lyonbrown4d/gity
      org.opencontainers.image.revision: "{{ .FullCommit }}"
      org.opencontainers.image.version: "{{ .Version }}"
  - <<: *gity_docker
    id: gity-worker
    ids:
      - gity-worker
    images:
      - ghcr.io/lyonbrown4d/gity-worker
    build_args:
      BINARY: gity-worker
    labels:
      org.opencontainers.image.title: gity-worker
      org.opencontainers.image.description: Gity background worker
      org.opencontainers.image.source: https://github.com/lyonbrown4d/gity
      org.opencontainers.image.revision: "{{ .FullCommit }}"
      org.opencontainers.image.version: "{{ .Version }}"
  - <<: *gity_docker
    id: gity-standalone
    ids:
      - gity-standalone
    images:
      - ghcr.io/lyonbrown4d/gity-standalone
    build_args:
      BINARY: gity-standalone
    labels:
      org.opencontainers.image.title: gity-standalone
      org.opencontainers.image.description: Gity single-process runtime
      org.opencontainers.image.source: https://github.com/lyonbrown4d/gity
      org.opencontainers.image.revision: "{{ .FullCommit }}"
      org.opencontainers.image.version: "{{ .Version }}"
  - <<: *gity_docker
    id: gity-runner
    ids:
      - gity-runner
    images:
      - ghcr.io/lyonbrown4d/gity-runner
    build_args:
      BINARY: gity-runner
    labels:
      org.opencontainers.image.title: gity-runner
      org.opencontainers.image.description: Gity CI runner agent
      org.opencontainers.image.source: https://github.com/lyonbrown4d/gity
      org.opencontainers.image.revision: "{{ .FullCommit }}"
      org.opencontainers.image.version: "{{ .Version }}"

checksum:
  name_template: checksums.txt

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
      - "^style:"

release:
  github:
    owner: lyonbrown4d
    name: gity
  draft: false
  prerelease: auto
  replace_existing_artifacts: true
  include_meta: true
