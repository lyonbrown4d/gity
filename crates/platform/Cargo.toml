[package]
name = "platform"
version = "0.1.0"
edition = "2024"

[dependencies]
models = { path = "../models" }
serde = { workspace = true, features = ["derive"] }
toasty = { workspace = true }
toasty-driver-postgresql = { workspace = true }
